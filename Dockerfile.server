FROM node:18-alpine
# Install required compilers and runtimes for local code execution
RUN apk add --no-cache python3 g++ openjdk11
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production
COPY . .
EXPOSE 5000
CMD ["node", "server.js"]
